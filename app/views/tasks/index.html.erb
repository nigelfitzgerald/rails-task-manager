<div class="container">
  <h4>My tasks</h4>
  <ul>
    <% @tasks.each do |task| %>
      <li><%= link_to task.title, task_path(task.id) %></li>
    <% end %>
  </ul>
</div>

<p><%= link_to '+ Add a new task', new_task_path %></p>
